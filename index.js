function submitData(name, email) {
  let userData = {
    name: name,
    email: email
  };
  
  let postObj = {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Accept': 'application/json'
    }
  }
}
